# AGENTS.md

## 项目概述

本工程用于扫描指定目录下的 PDF 文件，并将 PDF 中包含的图片及相关信息提取到目标目录中。项目应支持批量处理，适合对一批 PDF 文档进行图片资源归档、拆包或后续分析。

## 核心目标

- 递归或按配置扫描输入目录中的 PDF 文件。
- 从每个 PDF 中提取内嵌图片资源。
- 将提取结果写入指定输出目录。
- 保留足够的来源信息，方便追踪图片来自哪个 PDF、哪一页或哪个对象。
- 对无法解析、损坏或不包含图片的 PDF 给出清晰日志，不应导致整个任务异常中断。

## 建议输入输出约定

输入：

- 输入目录：待扫描 PDF 文件所在目录。
- 输出目录：提取图片和元数据的目标目录。
- 可选参数：是否递归扫描、是否覆盖已有文件、图片命名规则、日志级别等。

输出：

- 提取出的图片文件。
- 可选的元数据文件，例如 `metadata.json` 或 `metadata.csv`，记录：
  - 原始 PDF 路径
  - PDF 文件名
  - 页码
  - 图片序号
  - 图片格式
  - 图片宽高
  - 输出文件路径

推荐输出目录结构：

```text
output/
  example-pdf-name/
    page-001-image-001.png
    page-002-image-001.jpg
    metadata.json
```

## 开发原则

- 优先保持实现简单、可维护，避免引入与 PDF 图片提取无关的复杂抽象。
- 文件扫描、PDF 解析、图片写出、元数据记录应尽量保持职责分离。
- 文件名需要做安全处理，避免路径穿越、非法字符和同名覆盖问题。
- 批量处理时，一个文件失败不应影响其他 PDF 的处理。
- 对大文件应避免一次性加载不必要的数据，注意内存占用。
- 对输出目录中的已有文件，默认行为应明确：跳过、覆盖或自动重命名。

## 错误处理与日志

- 输入目录不存在、不可读时，应直接返回明确错误。
- 输出目录不存在时，可自动创建；创建失败时返回错误。
- 单个 PDF 解析失败时，应记录失败文件和原因，并继续处理其他文件。
- 没有发现 PDF 或没有提取到图片时，应输出清晰提示。
- 日志内容应包含处理进度、成功数量、失败数量和输出位置。

## 测试建议

新增或修改功能时，优先覆盖以下场景：

- 输入目录为空。
- 输入目录包含非 PDF 文件。
- 单个 PDF 不包含图片。
- 单个 PDF 包含多页、多张图片。
- 多个 PDF 批量处理。
- 输出文件重名处理。
- 损坏或无法解析的 PDF。
- 输出目录不存在时自动创建。

如项目使用 Go 实现，提交前建议运行：

```bash
go test ./...
```

## 代码风格

- 遵循当前仓库已有结构和命名习惯。
- Go 代码应使用 `gofmt` 或 `go fmt ./...` 格式化。
- 公共函数和复杂逻辑应包含简短注释，说明意图而不是重复代码。
- 不要提交测试产物、临时输出目录、大型 PDF 样本或提取出的图片文件，除非它们是明确需要的测试夹具。

## 后续实现参考

如果后续补充命令行入口，建议提供类似接口：

```bash
image-pack-extract --input ./pdfs --output ./output --recursive
```

命令执行结束后建议输出摘要：

```text
Processed PDFs: 12
Extracted images: 128
Failed PDFs: 1
Output: ./output
```
